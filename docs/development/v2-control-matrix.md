# V2 rendered control inventory and coverage

The [machine readable inventory](v2-control-inventory.json) records each rendered link, button, input, select, textarea, focusable control, disabled state, and accessible name on all eight populated demo routes. It also records the demo exercise list and tour, key dialogs and drawer, import preview, and blank dashboard. Generate it from the production demo with `node scripts/v2-inventory.mjs OUTPUT [FICTIONAL_WORKBOOK]`. The workbook argument must contain fictional data.

The initial route inventory was taken before source edits. Counts were Dashboard 37, Monthly 69, Budget 101, Debts 57, History 17, Accounts 80, Import 21, and Settings 13. The versioned inventory was regenerated after the fixes: 37, 73, 101, 57, 17, 80, 21, and 13 respectively. The four additional Monthly controls reflect the now reachable budget override state. Repeated shell and row controls are intentionally listed on every route where they render.

| Surface and actionable controls | Expected transition and data effect | Evidence |
| --- | --- | --- |
| Shell: eight navigation links, direct route, Back, active tab, Undo, disabled Undo, toast | Route changes preserve the current database; Undo restores the prior database state. | `demo.spec.ts` reset/undo/reload, `v2.spec.ts` V2-10 and dashboard Back navigation. |
| Demo: tour start/steps/skip/restart and keyboard, exercise links, reset, start blank, independent tab | Tour advances without data mutation; exercises open the target; reset/blank replace only the current tab database and are undoable. | `demo.spec.ts` tour and reset flows; `v2.spec.ts` V2-09. |
| Dashboard: month selector, Open reconciliation, six summary buttons, pending/review actions, Show all, Done checkboxes, budget/debt links | Navigation targets the selected month/account/entry; Done changes transfer state and derived status; Show all only changes display. | `v2.spec.ts` V2-03/04/05, dashboard drill-down; `demo.spec.ts` monthly completion. |
| History: month links and row actions | Open the selected month without changing stored amounts. | `v2.spec.ts` history link; `demo.spec.ts` historical data assertions. |
| Monthly: month selection/create/source, expected cash, allocations, account filter, Done, notes, budget link/refresh, journal ordinary/advanced add, edit, delete, debt link, CSV, close/reopen/override | Exact postings and reconciliation are recalculated; edits persist on reload; undo restores deleted entries; CSV reflects the journal; closed months are read-only. | `demo.spec.ts` allocation, transfer, debt link, complete/close; `v2.spec.ts` V2-01/03/05/07, journal CRUD/CSV; local private E2E month flow. |
| Budget: two tabs, version rows, draft/duplicate/activate/delete, metadata, payroll/deductions, categories/lines, tax rules | Version history is preserved; activation changes the active source for future months; existing months retain their copied allocations until refreshed. | `v2.spec.ts` V2-02/07 and version flow; local private E2E budget flow; unit/integration suite. |
| Debts: three summary buttons, account net links, six state tabs, account/search/date filters, reset, debt drawer, create/pay/adjust, event correct/delete, debt delete, CSV | Filters only affect display and CSV scope; event mutations recalculate subsequent balances and audit/undo; exported CSV matches displayed debt selection. | `demo.spec.ts` payment/overpayment; `v2.spec.ts` V2-06/08/09, filters/CSV and event roll-forward flow; local private E2E debt flow. |
| Accounts: create, row reorder, name, description, alias add/remove, color, archive/restore, delete, budget link | Edits persist in the local database; referenced codes cannot be deleted; deletion is confirmed and undoable. | `v2.spec.ts` account action/reload flow; account service unit tests. |
| Import/export: workbook selection, sample download, preview controls/reports, commit, JSON/Excel/CSV exports, backup selection/restore | Preview performs no commit; failed controls block import; commit preserves provenance; backup/restore recovers data. | `demo.spec.ts` sample and export/restore flows; local private E2E import controls; packaged macOS Open/Save pass. |
| Settings: desktop profile create/switch, safety-copy restore, audit display; demo storage explanation | Profiles remain separate; safety-copy restore makes another copy before replacing current data. | Packaged macOS pass on isolated profiles; `demo.spec.ts` tab isolation. |

The suite covers the listed **control families and critical data effects**. Repeated per-row instances and every editable budget/tax cell are not individually clicked in automated Chrome tests. Browser settings has no actionable controls because profiles and safety-copy actions exist only in the desktop app. The release report states these and other remaining limits explicitly.
